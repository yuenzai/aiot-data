```bash
python3 -m venv env
source env/bin/activate
python -m pip install --upgrade pip setuptools wheel
python -m pip list
python -m pip install "prefect[shell]==3.2.15"
python -m pip show prefect
```
